services:
  reality:
    build: ./reality
    volumes:
      - sock:/tmp
    restart: unless-stopped

  gateway:
    build: ./gateway
    ports:
      - "443:443"
    volumes:
      - sock:/tmp
      - ./config:/config
    depends_on:
      - reality
    restart: unless-stopped

volumes:
  sock:
